const symbols = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuvwxyz";
const digits = "0123456789";
const userAccountIdMappingTable = 'userAccountIdMapping';
const requestTable = 'userRequest';
const accountTable = 'userAccount';
const ledgerTable = 'userLedger';
const txnTable = 'userTxn';
const requestType = {
    deposit: 'deposit',
    withdraw: 'withdraw',
    register: 'register',
    collect: 'collect',
    pay: 'pay',
    transfer: 'transfer',
    seeSaved: 'seeSaved',
    sip: 'sip',
    bucket: 'bucket',
    findDeposit: 'findDeposit',
    findWithdraw: 'findWithdraw'
}
const txnStatus = {
    created: 'created',
    success: 'success',
    failed: 'failed',
}

const op = {
    debit: 'debit',
    credit: 'credit'
}

const txnUidSize = 6;
const txnUidRetryAttempts = 10;
const otpLen = 4;
const otpExpiryInSeconds = 5 * 60;

module.exports = {
    userAccountIdMappingTable,
    requestTable,
    accountTable,
    ledgerTable,
    txnTable,
    requestType,
    txnStatus,
    op,
    txnUidSize,
    txnUidRetryAttempts,
    otpLen,
    otpExpiryInSeconds,
    symbols,
    digits
}
